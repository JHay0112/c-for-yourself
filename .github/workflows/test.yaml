name: Run Tests

on: [push]


jobs:

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          chmod +x scripts/run_tests.sh
      - run: |
          scripts/run_tests.sh